import ReactOnRails from 'react-on-rails';

import Schedule from '../bundles/Schedule/components/Schedule';

// This is how react_on_rails can see the HelloWorld in the browser.
ReactOnRails.register({
  Schedule,
});
